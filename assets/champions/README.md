# Tournament winner photos

Drop the champion photos here — keep the `YEAR_caption.ext` names exactly as they are
(e.g. `2025_Congrats to Lower Perk on winning the SB-12 Championship!.jpeg`). The
year prefix is how they get grouped on the Champions page, and the caption becomes
the photo's caption. .jpg / .jpeg / .png all fine. I'll resize/optimize them after.
